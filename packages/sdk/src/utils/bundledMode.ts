/**
 * Node.js SDK build: not applicable; always false.
 */
export function isRunningWithBun(): boolean {
  return false
}

/**
 * Node.js SDK build: not applicable; always false.
 */
export function isInBundledMode(): boolean {
  return false
}
